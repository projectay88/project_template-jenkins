pipeline {
    agent {
            label "debian"
        }
    options {
            timestamps()
        }
    triggers { pollSCM('*****') }
    stages {
        stage('create docker image') {
            steps {
                echo '----docker image----'
                dir('.'){
                        sh 'docker build .'
                    }
            }
        }
            }
}
