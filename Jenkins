pipeline {
    agent {
            label "debian"
        }
    triggers { pollSCM('*') }
    stages {
        stage('create docker image') {
            steps {
                echo '----docker image----'
                dir('.'){
                        sh 'docker build .'
                    }
            }
        }
            }
}
