pipeline {
    agent {
            label "debian"
        }
    options {
            timestamps()
        }
    stages {
        stage('Hello') {
            steps {
                sh 'sudo apt-get update'
                sh 'sudo apt-get install ca-certificates curl gnupg'
            }
        }
            }
}
