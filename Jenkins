pipeline {
    agent {
            label "debian"
        }
    triggers { 
        cron('H/2****')
        }
    stages {
        stage('create docker image') {
            steps {
                echo '----docker image----'
                dir('.'){
                        sh 'docker build .'
                    }
            }
        }
            }
}
