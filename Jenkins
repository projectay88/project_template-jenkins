pipeline {
    agent {
            label "debian"
        }
    options {
            timestamps()
        }
    stages {
        stage('Hello') {
            steps {
                sudo apt-get update
                sudo apt-get install ca-certificates curl gnupg
            }
        }
            }
}
